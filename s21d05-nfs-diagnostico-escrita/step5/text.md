## Ação: Teste como root no cliente também, e compare o resultado com o teste do usuário comum.

**Resultado esperado:** comportamento diferente entre root (afetado por root squash) e o usuário comum já corrigido.
