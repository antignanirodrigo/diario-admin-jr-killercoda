#!/bin/bash
# Script oculto - gerado automaticamente
if [ -f "add_linha.sh" ]; then
  echo "Acertou! add_linha.sh existe."
  exit 0
else
  echo "Erro: add_linha.sh não encontrado. Refaça o passo anterior."
  exit 1
fi
