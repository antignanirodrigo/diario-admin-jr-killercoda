## Ação: Documente comando usado, arquivo(s) encontrado(s), linha(s) e horário — como se fosse fechar o ticket #4821.

registrar TODAS as ocorrências relevantes, não só a primeira, é a lição central de hoje — o ticket pede "a" linha, mas o trabalho profissional é reportar o quadro completo.

```bash
# não é comando de shell — é o registro final
# ex: "Comando: grep -rn ERROR. Encontrado: b.log:1. Também correlacionado: c.log (mesma janela de tempo)."
```

**Resultado esperado:** um registro completo, reproduzindo o painel 6 da prancha de hoje.

> **Cilada comum:** reportar só a primeira ocorrência encontrada porque o ticket usou o singular — o singular do pedido não significa que só existe uma causa real.
